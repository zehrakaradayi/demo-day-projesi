import { PlaceholderPage } from "@/components/placeholder-page";

export default function MeetupPage() {
  return (
    <PlaceholderPage
      title="Meetup Planla"
      briefSection="Bölüm 9, 17 · V2"
      description="Kişi sayısı + bütçe + şehir + rota + alternatifler. AI Meetup Planner buraya bağlanacak."
    />
  );
}
