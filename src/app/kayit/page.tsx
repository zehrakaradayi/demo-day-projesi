import { PlaceholderPage } from "@/components/placeholder-page";

export default function KayitPage() {
  return (
    <PlaceholderPage
      title="Kayıt Ol"
      briefSection="Auth · MVP"
      description="Supabase Auth ile kayıt akışı burada kurulacak. Başarılı kayıttan sonra kullanıcı /onboarding'e yönlendirilmeli."
    />
  );
}
